import SkeletonLoader from "@/components/SkeletonLoader";

/**
 * Next.js App Router loading UI.
 * Automatically shown during page navigation / lazy loading.
 */
export default function Loading() {
  return <SkeletonLoader />;
}
